import PrivacyPolicy from "@/components/PrivacyPolicy";
import React from "react";

type Props = {};

const Page = (props: Props) => {
  return (
    <div>
      <PrivacyPolicy />
    </div>
  );
};

export default Page;
